import { describe, it } from 'vitest';

// LEGACY_SKIP_V1: onboarding restore (WASM path removed; rewrite for account-keys)
describe.skip('Onboarding-restore.test (pending v1 rewrite)', () => {
  it('placeholder', () => {});
});
