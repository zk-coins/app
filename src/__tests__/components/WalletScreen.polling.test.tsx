import { describe, it } from 'vitest';

// LEGACY_SKIP_V1: wallet polling (legacy network/info shapes)
describe.skip('WalletScreen.polling.test (pending v1 rewrite)', () => {
  it('placeholder', () => {});
});
