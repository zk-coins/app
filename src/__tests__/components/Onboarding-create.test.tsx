import { describe, it } from 'vitest';

// LEGACY_SKIP_V1: onboarding create (WASM path removed; rewrite for account-keys)
describe.skip('Onboarding-create.test (pending v1 rewrite)', () => {
  it('placeholder', () => {});
});
