import { describe, it } from 'vitest';

// LEGACY_SKIP_V1: v1 live contract (legacy schemas removed)
describe.skip('contract.live.test (pending v1 rewrite)', () => {
  it('placeholder', () => {});
});
