import { describe, it } from 'vitest';

// LEGACY_SKIP_V1: v1 live smoke (legacy mint path removed)
describe.skip('node-smoke.live.test (pending v1 rewrite)', () => {
  it('placeholder', () => {});
});
